import { Renderer, ElementRef } from '@angular/core';
export declare class HsSlideComponent {
    private _renderer;
    private _element;
    constructor(_renderer: Renderer, _element: ElementRef);
    SetWidth(width: number): void;
}
